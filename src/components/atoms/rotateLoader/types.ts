import { color } from '@constants'

/**
 * Props for a rotating loader component.
 */
export interface IRotateLoaderProps {
  /**
   * The color of the loader (e.g., CSS color value).
   */
  color?: color
  /**
   * The width of the loader (e.g., CSS width value).
   */
  width?: string
}
